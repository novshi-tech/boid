# Binary
/boid

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store

# Test artifacts
*.test
coverage.out

# Logs
*.log

# boid project config (for self-testing)
*.local.*

# Claude Code runtime state / personal skills
/.claude/scheduled_tasks.lock
/.claude/skills/playwright-cli/

# Playwright CLI session data
/.playwright-cli/

# Phase 6 compose stack local env (host-specific paths; see .env.example)
/build/container/.env

# scripts/deploy-container.sh's own state: which engine (docker/podman)
# last brought the compose stack up, read back by --down (codex round-10
# review of PR5) — regenerated per-checkout, not tracked.
/.boid-compose-engine
