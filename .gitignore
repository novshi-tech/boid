# Binary
/boid
# クロスビルドの出力 (GOOS=windows go build . は boid.exe を吐く)。
# 検証時は -o /dev/null を使うこと。
/boid.exe
*.exe

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store

# Test artifacts
*.test
coverage.out

# Logs
*.log

# boid project config (for self-testing)
*.local.*

# Claude Code runtime state / personal skills
/.claude/scheduled_tasks.lock
/.claude/skills/playwright-cli/

# Playwright CLI session data
/.playwright-cli/

# Phase 6 compose stack local env (host-specific paths; see .env.example)
/build/container/.env
