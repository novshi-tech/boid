env:
  E2E_STATE_DIR: ${E2E_STATE_DIR}
hooks:
  - id: build-artifact
    on: executing
gates:
  - id: verify-resolved
    on: verifying
    traits:
      consumes: [artifact]
  - id: feedback-cycle
    on: collecting_feedback
    traits:
      consumes: [artifact]
