env:
  E2E_STATE_DIR: ${E2E_STATE_DIR}
hooks:
  - id: emit-artifact
    on: executing
  - id: emit-verification
    on: executing
gates:
  - id: validate-a
    on: verifying
    requires_traits:
      - artifact
  - id: validate-b
    on: verifying
    requires_traits:
      - artifact
