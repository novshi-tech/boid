This file only exists so the harness has content to seed on the unauthorized
bare repo upstream. Its content must never actually reach any hook that
runs inside the sandbox — the gateway is expected to deny access.
